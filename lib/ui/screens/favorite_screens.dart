import 'package:flutter/material.dart';

class FavoriteScreens extends StatelessWidget {
  const FavoriteScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("This is favorite screen"),);
  }
}